# Phase 1–2 (extend for Streamlit / Phase 3+ as needed)
# Albumentations >=1.4.19 (and 2.x) depend on stringzilla via albucore; stringzilla often
# has no wheel for Python 3.14 and falls back to a C++ build (needs MSVC on Windows).
# Stay on <=1.4.18 (or 1.3.x) to avoid that.
torch>=2.0
torchvision
segmentation-models-pytorch
albumentations>=1.3.1,<=1.4.18
opencv-python-headless
numpy
scipy
matplotlib
pandas
plotly
streamlit
